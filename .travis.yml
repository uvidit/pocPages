language: java
sudo: false

script: echo Helloooo
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  on:
    branch: master
  local_dir: public
  repo:  uvidit/pocProjectDocPages.git
  target-branch: gh-pages
